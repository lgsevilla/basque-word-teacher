package com.eguneko.hitza.repository;

import com.eguneko.hitza.model.BasqueWord;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BasqueWordRepository extends JpaRepository<BasqueWord, Long> {
}
